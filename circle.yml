deployment:
  staging:
    branch: master
    heroku:
      appname: hiddenite

machine:
  java:
    version: oraclejdk8
  environment:
    JDBC_DATABASE_URL: jdbc:mysql://localhost:3306/circle_test?user=ubuntu&password=
